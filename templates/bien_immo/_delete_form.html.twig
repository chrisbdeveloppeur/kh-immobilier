<form class="has-text-right-desktop has-text-right-desktop" method="post" action="{{ path('bien_immo_delete', {'id': bien_immo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bien_immo.id) }}">
    <button class="button is-danger mx-3 my-2">Supprimer ce bien</button>
</form>
