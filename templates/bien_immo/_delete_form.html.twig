<form method="post" action="{{ path('bien_immo_delete', {'id': bien_immo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bien_immo.id) }}">
    <button class="btn">Supprimer ce bien</button>
</form>
