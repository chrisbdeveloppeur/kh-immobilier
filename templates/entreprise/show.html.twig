{% extends 'base.html.twig' %}

{% block title %}{{ entreprise.name }}{% endblock %}
{% block title_main %}{{ entreprise.name }}{% endblock %}

{% block body %}

    <div class="table-container">
        <table class="table">
            <tbody>
            <tr>
                <th>Titre</th>
                <td>{{ entreprise.name }}</td>
            </tr>
            <tr>
                <th>SIRET</th>
                <td>{{ entreprise.siret }}</td>
            </tr>
            <tr>
                <th>N° TVA</th>
                <td>{{ entreprise.tvaNumber }}</td>
            </tr>
            </tbody>
        </table>

    </div>

    <a href="{{ path('entreprise_index') }}">Retour à la lise des entreprises</a>

    <a href="{{ path('entreprise_edit', {'id': entreprise.id}) }}">Modifier</a>

    {{ include('entreprise/_delete_form.html.twig') }}
{% endblock %}
