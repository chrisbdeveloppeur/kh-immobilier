{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
    <section class="hero is-info is-fullheight">
        <div class="hero-body">
            <div class="column py-0 px-0 has-text-centered is-flex is-justify-content-center is-white">
                {{ form_start(form, {'attr': {'class': 'is-half column py-0 px-0'}}) }}
                <div class="field">
                    <div class="control has-icons-left has-icons-right">
                        {{ form_widget(form.client_name, {'attr': {'class':'input has-text-centered', 'placeholder': 'Nom du client'}}) }}
                        <span class="icon is-left">
                            <i class="fas fa-user"></i>
                        </span>
{#                        <span class="icon is-right">#}
{#                          <i class="fas fa-check"></i>#}
{#                        </span>#}
                    </div>
                </div>

                <div class="field">
                    <div class="control has-icons-left has-icons-right">
                        {{ form_widget(form.phone, {'attr': {'class':'input has-text-centered', 'placeholder': 'Numéro de telephone'}}) }}
                        <span class="icon is-left">
                            <i class="fas fa-phone"></i>
                        </span>
                        {#                        <span class="icon is-right">#}
                        {#                          <i class="fas fa-check"></i>#}
                        {#                        </span>#}
                    </div>
                </div>

                <div class="field">
                    <div class="control has-icons-left has-icons-right">
                        {{ form_widget(form.email, {'attr': {'class':'input has-text-centered', 'placeholder': 'Adresse email'}}) }}
                        <span class="icon is-left">
                            <i class="fas fa-mail-bulk"></i>
                        </span>
                        {#                        <span class="icon is-right">#}
                        {#                          <i class="fas fa-check"></i>#}
                        {#                        </span>#}
                    </div>
                </div>

                <div class="field">
                    <div class="control has-icons-left has-icons-right">
                        {{ form_widget(form.adresse, {'attr': {'class':'input has-text-centered', 'placeholder': 'Adresse postale'}}) }}
                        <span class="icon is-left">
                            <i class="fas fa-location-arrow"></i>
                        </span>
                        {#                        <span class="icon is-right">#}
                        {#                          <i class="fas fa-check"></i>#}
                        {#                        </span>#}
                    </div>
                </div>

                <div class="field">
                    <div class="control">
                        {{ form_widget(form.description_1, {'attr': {'class':'textarea has-text-centered', 'placeholder': 'Description du produit / service', 'style':'resize:none;'}}) }}
                    </div>
                </div>

                <div class="field">
                    <div class="control has-icons-left has-icons-right">
                        {{ form_widget(form.quantity_1, {'attr': {'class':'input has-text-centered', 'placeholder': 'Quantité'}}) }}
                        <span class="icon is-left">
                            <i class="fas fa-sort-numeric-asc"></i>
                        </span>
                        {#                        <span class="icon is-right">#}
                        {#                          <i class="fas fa-check"></i>#}
                        {#                        </span>#}
                    </div>
                </div>

                <div class="field">
                    <div class="control has-icons-left has-icons-right">
                        {{ form_widget(form.price_unit_ht_1, {'attr': {'class':'input has-text-centered', 'placeholder': 'Prix unitaire HT'}}) }}
                        <span class="icon is-left">
                            <i class="fas fa-money"></i>
                        </span>
                        {#                        <span class="icon is-right">#}
                        {#                          <i class="fas fa-check"></i>#}
                        {#                        </span>#}
                    </div>
                </div>

                <div class="field">
                    <div class="control has-icons-left has-icons-right">
                        {{ form_widget(form.total_ht_1, {'attr': {'class':'input has-text-centered', 'placeholder': 'Total HT'}}) }}
                        <span class="icon is-left">
                            <i class="fas fa-object-group"></i>
                        </span>
                        {#                        <span class="icon is-right">#}
                        {#                          <i class="fas fa-check"></i>#}
                        {#                        </span>#}
                    </div>
                </div>

                <div class="control">
                    <button class="button is-success">Editer le devis</button>
                </div>

                {{ form_end(form) }}
{#                <a href="{{ path('edit_word') }}" class="button">Editer le fichier word</a>#}
            </div>
        </div>
    </section>
{% endblock %}
