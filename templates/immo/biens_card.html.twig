<div class="card">
    <header class="card-header">
        <p class="card-header-title">
            {{ bien_immo.building }}
        </p>
    </header>
    <div class="card-content">
        <div class="content">
            Adresse : <b>{{ bien_immo.street }}, {{ bien_immo.cp }} {{ bien_immo.city }}</b><br>
            Loyer : <b>{{ bien_immo.loyerHc }} €</b><br>
            Charges : <b>{{ bien_immo.charges }} €</b><br>
            Loyer TTC : <b>{{ bien_immo.loyerHc + bien_immo.charges }} €</b><br>
            Locataire : <a href="{{ path('locataire_show', {'id': bien_immo.locataires.current.id}) }}" class="tag is-dark">{{ bien_immo.locataires.current.firstName }} {{ bien_immo.locataires.current.lastName }}</a>
        </div>
    </div>
    <footer class="card-footer has-background-dark">
        <a href="{{ path('bien_immo_show', {'id': bien_immo.id}) }}" class="card-footer-item has-text-light">Détails</a>
        <a href="{{ path('bien_immo_edit', {'id': bien_immo.id}) }}" class="card-footer-item has-text-light">Gérer ce logement</a>
{#        <a href="#" class="card-footer-item">Delete</a>#}
    </footer>
</div>