<form class="has-text-centered" method="post" action="{{ path('locataire_delete', {'id': locataire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ locataire.id) }}">
    <button class="button is-danger">Supprimer le locataire</button>
</form>
