<form class="has-text-right-desktop has-text-right-desktop" method="post" action="{{ path('locataire_delete', {'id': locataire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ locataire.id) }}">
    <button class="button is-danger is-small mx-3 my-2">Supprimer le locataire</button>
</form>
