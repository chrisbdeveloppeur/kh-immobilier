{#<form class="has-text-centered" method="post" action="{{ path('user_delete', {'id': user.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer ce gestionnaire');">#}
{#    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">#}
{#    <button class="button is-danger is-small">Supprimer ce gestionnaire</button>#}
{#</form>#}

<div class="column has-text-centered">
    <a href="{{ path('user_del', {'id': user.id }) }}" class="button is-small is-danger" onclick="return confirm('Êtes-vous sûr de vouloir supprimer définitivement ce compte gestionnaire ? Le compte sera irrécupérable')">
        <span class="icon">
            <i class="fas fa-user-times"></i>
        </span><span>
            Supprimer le compte
        </span>
    </a>
</div>
