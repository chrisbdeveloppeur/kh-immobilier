{% extends 'base.html.twig' %}

{% block title %}{{ user }}{% endblock %}
{% block title_main %}<b>{{ user }}</b>{% endblock %}

{% block body %}

    {{ include('themes/form/_form.html.twig', {'button_label': 'Valider'}) }}

    {% if is_granted('ROLE_SUPER_ADMIN') %}
        {{ include('user/_delete_form.html.twig') }}
    {% endif %}

{% endblock %}
